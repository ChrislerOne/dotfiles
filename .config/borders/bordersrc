#!/bin/bash

options=(
  style=round
  width=5.0
  ax_focus=on
  active_color=0xff89b4fa   # Catppuccin Mocha Blue
  inactive_color=0x00000000 # Transparent — prevents ghost borders with AeroSpace
)

borders "${options[@]}"
